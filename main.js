const { boss1 } = require('./characters')
const { player1 } = require('./characters')
const { playGame } = require('./playgame')

    playGame()
   
    module.exports = {
        
        player1,
        boss1
    }